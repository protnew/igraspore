# GitHub / дерево — аудит секретов и ПДн — 2026-09-22

| Поле | Значение |
|---|---|
| Репозиторий | https://github.com/protnew/igraspore.git |
| Корень | `C:\Obsidian\New\Projects\08-iGraSpore_V2` |
| Метод | `git ls-files` + поиск типичных имён секретов (значения не копировались) |

## Находки

| Статус | Что | Путь |
|---|---|---|
| **ВЫЧИСТИТЬ** | Keystore подписи APK в tracking | `06-DevOps-and-Release/apk/igraspore.keystore` |
| Чисто (по имени) | Нет `.env` / `*.pem` / `id_rsa` в индексе git | — |
| Замечание | `*.keystore` **нет** в `.gitignore` | `.gitignore` |

Поиск ПДн (email/телефон) по HEAD в этом прогоне не выгружал содержимое файлов в отчёт — полный текстовый grep при необходимости отдельно. Критичный блокер для публичной выкладки APK: keystore в репо.

## Обязательно до выкладки нового APK

1. Новый keystore **вне** `C:\Obsidian\` (цель: `%USERPROFILE%\.secrets\igraspore\igraspore-release.keystore`).
2. Пароли только в `%USERPROFILE%\.secrets\igraspore\` (не в md/чат/git).
3. `git rm --cached` старого keystore + строка `*.keystore` в `.gitignore`.
4. Полный `git filter-repo` истории — **только** после отдельного «да» Алексея (в ТЗ).

## Итог пакета E (этап аудит)

**Проблема.** В открытом контуре лежит материал подписи.

**Решение.** Зафиксировать находку; ротация ключа и снятие с tracking — следующие шаги исполнения.

**Результат.** Список «чисто / вычистить» понятен; блокер один — keystore.