# Сверка ТЗ v3 с диском — 2026-09-22

| Поле | Значение |
|---|---|
| ТЗ | `C:\Obsidian\New\Projects\08-iGraSpore_V2\09-Docs\out-dev-factory\TZ.md` |
| Версия ТЗ | v3 |
| Итог | ТЗ соответствует фактам; противоречий по решениям §2 нет. Можно исполнять пакеты. |

## Светофор документа vs факт

| Пакет | Формулировка в ТЗ | На диске / в сети | Готово? |
|---|---|---|---|
| A | Новый ключ + честный APK | keystore в git; `/igraspore.apk` → `text/html`; кнопка = div+onclick | Нет |
| B | Один код + архив | 34/34 js; **13 DIFF**, везде новее `.04-Src`; index грузит `04-Src`; снимка visible нет | Нет |
| C | WebGL стиль | `renderMode` cartoon/swiss есть; `.04-Src\v3-webgl` есть; webgl в settings нет | Нет |
| D | Только CF | remote github.com/protnew/igraspore; GH Pages ещё отдаёт HTML (13.08) | Нет |
| E | Аудит GitHub | отчёт рядом (GITHUB-AUDIT-2026-09-22.md) | В работе |
| F | Бэклог | `08-Backlog\backlog_iGraSpore_V2.db` (385024 байт) | Нет |
| G | Choser | (исполнение позже) | Нет |
| H | a11y | APK не semantic; focus-visible не проверен в этом прогоне | Нет |

## JS: 04-Src vs .04-Src

Совпадают: 21 файл. Расходятся (канон = .04-Src, новее):  
`ai_move.js`, `biology_divide.js`, `biology_virus.js`, `config.js`, `night_sky.js`, `render.js`, `render_effects.js`, `render_entities.js`, `render_fx.js`, `render_surface.js`, `ui_food_chain.js`, `ui_menus.js`, `world.js`.

## Прочее

- `.10-Archive` есть; снимка `04-Src-visible-snapshot-2026-09-22` нет.
- `.04-Src\v3-webgl` = True.
- Локальные APK: `igraspore-offline-v2.4.apk` (15.08, ~4.8 MB) и др.
- `index.html` stamp `v=1786825630`, scripts `04-Src/js/...`.
- Сеть (с box): `https://igraspore.pages.dev/igraspore.apk` → Content-Type **text/html** (как в ТЗ).

## Решение по способу канона (пакет B)

**Проблема.** CF/часть тулов плохо дружит с путями `.04-Src` в публичном URL.

**Решение.** Канон разработки = `.04-Src`. Для runtime сайта: `index.html` и деплой грузят путь `.04-Src/js` локально в Obsidian-дереве; при выкладке на CF — явная копия канона в publish-dir без скрытой точки (зафиксируем в отчёте хода пакета A/D). Видимый `04-Src/js` уходит в архив, на месте — README.

**Результат.** Одна правда кода; Obsidian не индексирует тяжёлый видимый дубль.