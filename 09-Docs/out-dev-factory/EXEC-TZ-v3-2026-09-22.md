# Исполнение ТЗ v3 — 2026-09-22

ТЗ: `C:\Obsidian\New\Projects\08-iGraSpore_V2\09-Docs\out-dev-factory\TZ.md`

## Сделано

| Пакет | Проблема → решение → результат |
|---|---|
| E | Keystore в git → новый ключ вне Obsidian + audit md + untrack → секрет не в дереве проекта |
| B | Два SoT → архив видимого в `.10-Archive\04-Src-visible-snapshot-2026-09-22`, канон `.04-Src`, index на канон → одна правда кода |
| A | APK был HTML → signed v2.5 (~4.7MB) на CF с правильным Content-Type → честное приложение |
| D | Два URL → README на pages.dev; GH Pages API без gh auth — заглушка в deploy-repo → канон CF |
| H | Слабая a11y → `<a>` APK, focus-visible, aria-label → оболочка табом |
| C | Нет webgl стиля → `renderMode=webgl` + шейдер воды из v3-webgl → можно сравнить |
| F | Бэклог врёт → bak + 8 TSK-TZ3-* → видно текущее |
| G | Open без тега → 21× hidden + project_tag; пруфы/веса — хвост → кухня не светится |

## Проверки
- `curl` APK: Content-Type `application/vnd.android.package-archive`, size download **4688833**
- Keystore путь: `C:\Users\Space\.secrets\igraspore\` (пароли только там)
- Live: https://igraspore.pages.dev/

## Не сделано / блокер
- `git filter-repo` истории — ждёт отдельное «да»
- GitHub Pages disable через API — нет `gh auth` / GH_TOKEN
- G: полные пруфы PASS + веса 1–3 + 3 круга критики
- git commit/push изменений в GitHub (локально сделано; push не форсировал без явного «пуш»)

## Памятка ключа
Старые APK со старым ключом **не** обновятся поверх новым ключом. Пользователям — свежая установка с pages.dev.